@model IEnumerable<NinthProject.Groups>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Related";
}

<h1>Related</h1>

<div>
    <h4>Groups</h4>
    <hr />
@foreach (var item in Model) 
{
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(item => item.CourseId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(modelItem => item.CourseId)
        </dd>
       <dt class = "col-sm-2">
            @Html.DisplayNameFor(item => item.GroupName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(modelItem => item.GroupName)
        </dd>
        </dl>
}
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
